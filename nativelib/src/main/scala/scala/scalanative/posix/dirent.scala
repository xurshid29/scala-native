@extern
object dirent {
   // DIR *opendir (const char *__name) __nonnull ((1));
   // DIR *fdopendir (int __fd);
   // DIR *fdopendir (int __fd);
   // struct dirent *readdir (DIR *__dirp) __nonnull ((1));
   // void rewinddir (DIR *__dirp) __THROW __nonnull ((1));
   // void seekdir (DIR *__dirp, long int __pos) __THROW __nonnull ((1));
   // long int telldir (DIR *__dirp) __THROW __nonnull ((1));
}
