package scala.scalanative
package runtime

import native._

%{
    primitives = ['Boolean', 'Char', 'Byte', 'Short',
                  'Int', 'Long', 'Float', 'Double', 'Unit', 'Ptr']
}%

% for T in primitives:

class Primitive${T} private ()

% end
